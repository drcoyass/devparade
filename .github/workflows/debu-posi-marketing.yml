name: DebuPosi Marketing - Auto Tweet Generator

on:
  workflow_dispatch:
    inputs:
      campaign:
        description: 'キャンペーン名 (launch / scheduled / collab)'
        required: false
        default: 'scheduled'
  schedule:
    - cron: '0 3 * * 1,4'  # 月・木 12:00 JST - 週2回自動生成

permissions:
  issues: write

jobs:
  generate-tweet:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Generate tweet content
        env:
          CAMPAIGN: ${{ github.event.inputs.campaign || 'scheduled' }}
        run: python scripts/debu-posi-tweet-generator.py

      - name: Create Issue with tweet
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          if [ -f tweet_issue.md ]; then
            DATE=$(date +%Y-%m-%d)
            gh issue create \
              --title "🍖 [デブポジツイート] ${DATE}" \
              --body "$(cat tweet_issue.md)" \
              --label "tweet-ready" || \
            gh issue create \
              --title "🍖 [デブポジツイート] ${DATE}" \
              --body "$(cat tweet_issue.md)"
          fi
